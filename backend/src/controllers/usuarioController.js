// Ejemplo de controlador para usuarios

exports.getUsuarios = (req, res) => {
  // Aquí iría la lógica para obtener usuarios desde la base de datos
  res.json({ message: 'Controlador: obtener usuarios' });
};
